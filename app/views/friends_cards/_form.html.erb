<%= bootstrap_form_for(@friends_card) do |f| %>
  <% if @friends_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@friends_card.errors.count, "error") %> prohibited this friends_card from being saved:</h2>

      <ul>
      <% @friends_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.email_field :email %>
  </div>
  <div class="field">
    <%= f.text_field :ic_number %>
  </div>
  <div class="field">
    <%= f.date_select :date_of_birth %>
  </div>
  <div class="field">
    <%= f.text_field :contact_number %>
  </div>
  <div class="field">
    <%= f.text_area :shipping_address %>
  </div>
  <div class="field">
    <%= f.check_box :gift %>
  </div>
  <div class="actions">
    <%= f.submit "Subscribe" %>
  </div>
<% end %>
