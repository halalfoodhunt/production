$("ul.errors").html("")
<% if @culinary_skill.errors.any? %>
  <% @culinary_skill.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>
  $("#culinary-skill-modal").modal("hide")
<% end %>