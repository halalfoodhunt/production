<%= form_for(@product_source) do |f| %>
  <% if @product_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_source.errors.count, "error") %> prohibited this product_source from being saved:</h2>

      <ul>
      <% @product_source.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
