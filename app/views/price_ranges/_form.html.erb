<div class="modal-body">
  <%= bootstrap_form_for(@price_range) do |f| %>
  <div class="modal-body">
    <% if @price_range.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@price_range.errors.count, "error") %> prohibited this price range from being saved:</h2>
      <ul>
        <% @price_range.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    </div>
    <% end %>
    <div class="field">
      <%= f.text_field :price %>
    </div>
        <div class="field">
      <%= f.text_field :dollar_sign %>
    </div>
    <div class="modal-footer">
     <%= f.submit :class => "btn btn-block btn-raised btn-material-teal" %>
     <%= link_to "Cancel", "#", class: "btn", data: {dismiss: "modal"} %>
   </div>
 </div>
 <% end %>
</div>