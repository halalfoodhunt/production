$("ul.errors").html("")
<% if @establishment_type.errors.any? %>
  <% @establishment_type.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>
  $("#establishment_types-modal").modal("hide")
<% end %>