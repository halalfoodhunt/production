<div class="modal-body">
  <%= bootstrap_form_for(@lesson_type) do |f| %>
  <div class="modal-body">
    <% if @lesson_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lesson_type.errors.count, "error") %> prohibited this lesson type from being saved:</h2>
      <ul>
        <% @lesson_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    </div>
    <% end %>
    <div class="field">
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.file_field :icon %>
    </div>
    <div class="modal-footer">
     <%= f.submit :class => "btn btn-block btn-raised btn-material-teal" %>
     <%= link_to "Cancel", "#", class: "btn", data: {dismiss: "modal"} %>
   </div>
 </div>
 <% end %>
</div>