<%= bootstrap_form_for(@merchant, as: @merchant, url: merchant_registration_path(@merchant), label_errors: true, inline_errors: true) do |f| %>
<%= f.select(:role, Merchant.roles.keys.map {|role| [role.titleize,role]}) %>
<% end %>