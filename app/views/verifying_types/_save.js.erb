$("ul.errors").html("")
<% if @verifying_types.errors.any? %>
  <% @verifying_type.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>
  $("#verifying_types-modal").modal("hide")
<% end %>