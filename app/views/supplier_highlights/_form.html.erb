<div class="modal-body">
  <%= bootstrap_form_for(@supplier_highlight) do |f| %>
  <div class="modal-body">
    <% if @supplier_highlight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supplier_highlight.errors.count, "error") %> prohibited this supplier highlight from being saved:</h2>
      <ul>
        <% @supplier_highlight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    </div>
    <% end %>
    <div class="field">
      <%= f.text_field :name %>
    </div>
    <div class="modal-footer">
     <%= f.submit :class => "btn btn-block btn-raised btn-material-teal" %>
     <%= link_to "Cancel", "#", class: "btn", data: {dismiss: "modal"} %>
   </div>
 </div>
 <% end %>
</div>