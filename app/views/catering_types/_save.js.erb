$("ul.errors").html("")
<% if @catering_type.errors.any? %>
  <% @catering_type.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>
  $("#catering-type-modal").modal("hide")
<% end %>