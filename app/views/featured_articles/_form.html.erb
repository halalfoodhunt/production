<%= form_for(@featured_article) do |f| %>
  <% if @featured_article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@featured_article.errors.count, "error") %> prohibited this featured_article from being saved:</h2>

      <ul>
      <% @featured_article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :article_url %><br>
    <%= f.text_field :article_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
