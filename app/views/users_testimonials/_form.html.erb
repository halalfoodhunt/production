<%= form_for(@users_testimonial) do |f| %>
  <% if @users_testimonial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@users_testimonial.errors.count, "error") %> prohibited this users_testimonial from being saved:</h2>

      <ul>
      <% @users_testimonial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :testimonial %><br>
    <%= f.text_area :testimonial %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
