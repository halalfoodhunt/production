$("ul.errors").html("")
<% if @food_delivery_type.errors.any? %>
  <% @food_delivery_type.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>
  $("#food-delivery-type-modal").modal("hide")
<% end %>