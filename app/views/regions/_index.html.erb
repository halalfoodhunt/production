 <% @regions.each do |regions| %>
 <tr>
  <td class="col-md-9"><%= regions.name %></td>
  <td class="col-md-1"><%= link_to edit_region_path(regions), remote: true do %>
    <i class="fa fa-pencil" style="color:brown"></i><% end %>
  </td>
  <td class="col-md-1"><%= link_to regions, method: :delete, data: { confirm: 'Are you sure?' } do %>
    <i class="fa fa-trash-o" style="color:brown"></i><% end %>
  </td>
</tr>
<% end %>