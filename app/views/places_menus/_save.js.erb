$("ul.errors").html("")
<% if @places_menu.errors.any? %>
  <% @places_menu.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>
  $("#places-menu-modal").modal("hide")
<% end %>