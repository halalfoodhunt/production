$("ul.errors").html("")
<% if @locations.errors.any? %>
  <% @locations.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>
  $("#location-modal").modal("hide")
<% end %>