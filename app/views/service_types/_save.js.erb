$("ul.errors").html("")
<% if @service_type.errors.any? %>
  <% @service_type.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>
  $("#service_types-modal").modal("hide")
<% end %>