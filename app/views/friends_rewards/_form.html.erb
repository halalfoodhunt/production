<div class="modal-body">
  <%= bootstrap_form_for(@friends_reward) do |f| %>
  <div class="modal-body">
    <% if @friends_reward.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@friends_reward.errors.count, "error") %> prohibited this friends reward from being saved:</h2>
      <ul>
        <% @friends_reward.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    </div>
    <% end %>
    <div class="field">
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.file_field :icon %>
    </div>
    <div class="modal-footer">
     <%= f.submit :class => "btn btn-block btn-raised btn-material-teal" %>
     <%= link_to "Cancel", "#", class: "btn", data: {dismiss: "modal"} %>
   </div>
 </div>
 <% end %>
</div>