$("ul.errors").html("")
<% if @food_type.errors.any? %>
  <% @food_type.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>
  $("#food-type-modal").modal("hide")
<% end %>