<div class="modal-body">
  <%= bootstrap_form_for(@supermarket_highlight) do |f| %>
  <div class="modal-body">
    <% if @supermarket_highlight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supermarket_highlight.errors.count, "error") %> prohibited this supermarket_hightlight from being saved:</h2>
      <ul>
        <% @supermarket_highlight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    </div>
    <% end %>
    <div class="field">
      <%= f.text_field :name, autofocus: true %>
    </div>
    <div class="field">
      <%= f.file_field :icon %>
    </div>
    <div class="modal-footer">
     <%= f.submit :class => "btn btn-block btn-raised btn-material-teal" %>
     <%= link_to "Cancel", "#", class: "btn", data: {dismiss: "modal"} %>
   </div>
</div>
<% end %>
</div>
